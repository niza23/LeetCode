class Solution {
public:
    int strStr(string haystack, string needle) {
        
        if(needle=="")
            return 0;
        int found = haystack.find(needle); //in case if doesn't find anything it will return 'npos'
        
        if(found!=string::npos)
            return found;
        return -1;
        
    }
};